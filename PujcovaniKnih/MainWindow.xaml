<Window x:Class="PujcovaniKnih.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PujcovaniKnih.ViewModels"
        xmlns:local="clr-namespace:PujcovaniKnih.Views"
        mc:Ignorable="d"
        Title="Knihovní systém" Height="550" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <TabControl Margin="5">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </TabControl.Resources>

            <TabItem Header="Knihy">
                <local:BooksView DataContext="{Binding BooksVM}"/>
            </TabItem>

            <TabItem Header="Zákazníci">
                <local:CustomersView DataContext="{Binding CustomersVM}"/>
            </TabItem>

            <TabItem Header="Výpůjčky">
                <local:LoansView DataContext="{Binding LoansVM}"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
