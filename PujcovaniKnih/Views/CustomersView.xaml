<UserControl x:Class="PujcovaniKnih.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!-- DataGrid: list of customers -->
        <DataGrid Grid.Row="0" ItemsSource="{Binding Customers}" 
                  SelectedItem="{Binding SelectedCustomer}" 
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Form and buttons -->
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="Name:" Width="50"/>
                <TextBox Text="{Binding SelectedCustomer.Name, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                <TextBlock Text="Email:" Width="50" Margin="10,0,0,0"/>
                <TextBox Text="{Binding SelectedCustomer.Email, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                <TextBlock Text="Phone:" Width="50" Margin="10,0,0,0"/>
                <TextBox Text="{Binding SelectedCustomer.Phone, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,0">
                <Button Content="Add" Width="75" Margin="0,0,5,0" Command="{Binding AddCustomerCommand}"/>
                <Button Content="Update" Width="75" Margin="0,0,5,0" Command="{Binding UpdateCustomerCommand}"/>
                <Button Content="Delete" Width="75" Command="{Binding DeleteCustomerCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>