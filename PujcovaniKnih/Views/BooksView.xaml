<UserControl x:Class="PujcovaniKnih.Views.BooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Evidence knih" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>

            <TextBlock Text="Hledat:" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="2" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalContentAlignment="Center" Padding="5"/>
        </Grid>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Books}" 
                  SelectedItem="{Binding SelectedBook}" 
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Margin="0,0,0,20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Název knihy" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Autor" Binding="{Binding Author}" Width="*"/>
                <DataGridCheckBoxColumn Header="Dostupná" Binding="{Binding IsAvailable}" Width="80" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="15" Background="#f9f9f9">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Název knihy:" VerticalAlignment="Center" Margin="0,0,10,10" Grid.Row="0" Grid.Column="0"/>
                <TextBox Text="{Binding SelectedBook.Title, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="0,0,20,10" Padding="3"/>

                <TextBlock Text="Autor:" VerticalAlignment="Center" Margin="0,0,10,10" Grid.Row="0" Grid.Column="2"/>
                <TextBox Text="{Binding SelectedBook.Author, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="3" Margin="0,0,0,10" Padding="3"/>

                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.ColumnSpan="4" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Nová kniha (Vyčistit)" Command="{Binding NewCommand}" Width="140" Margin="0,0,10,0"/>
                    <Button Content="Uložit" Command="{Binding SaveCommand}" Width="100" Margin="0,0,10,0" FontWeight="Bold"/>
                    <Button Content="Smazat" Command="{Binding DeleteCommand}" Width="80" Background="#FFCCCC"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>