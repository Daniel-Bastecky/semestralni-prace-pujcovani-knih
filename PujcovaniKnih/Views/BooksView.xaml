<UserControl x:Class="PujcovaniKnih.Views.BooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!-- DataGrid: shows all books -->
        <DataGrid Grid.Row="0" ItemsSource="{Binding Books}" 
                  SelectedItem="{Binding SelectedBook}" 
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}"/>
                <DataGridTextColumn Header="Author" Binding="{Binding Author}"/>
                <DataGridCheckBoxColumn Header="Available" Binding="{Binding IsAvailable}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Form and buttons for adding/editing/deleting books -->
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="Title:" Width="50"/>
                <TextBox Text="{Binding SelectedBook.Title, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                <TextBlock Text="Author:" Width="50" Margin="10,0,0,0"/>
                <TextBox Text="{Binding SelectedBook.Author, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                <CheckBox IsChecked="{Binding SelectedBook.IsAvailable}" Content="Available" Margin="10,0,0,0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,0">
                <Button Content="Add" Width="75" Margin="0,0,5,0" Command="{Binding AddBookCommand}"/>
                <Button Content="Update" Width="75" Margin="0,0,5,0" Command="{Binding UpdateBookCommand}"/>
                <Button Content="Delete" Width="75" Command="{Binding DeleteBookCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
